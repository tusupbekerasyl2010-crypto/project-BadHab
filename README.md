# Turtle Ping Pong

Controls:
- Player A: `w` (up), `s` (down)
- Player B: `Up` (up), `Down` (down)
- Quit: `q`

Run:

```bash
python3 ping_pong.py
```

This launches a simple two-player ping-pong game using Python's `turtle` module.
